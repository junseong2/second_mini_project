
#bowling 카테고리 추가함으로 인해
#bowling 에는 gSize 및 gColor 수정이 안되도록 "기본" 으로 설정되게 함
#원래 gSize는 CHAR(1) 이었는데 2글자인 기본이 되어야하므로 VARCHAR(10)으로 수정

ALTER TABLE cart
MODIFY COLUMN gSize VARCHAR(10) NOT NULL;

ALTER TABLE orderinfo
MODIFY COLUMN gSize VARCHAR(10) NOT NULL;